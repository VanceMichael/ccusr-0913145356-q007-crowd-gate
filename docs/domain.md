# 纪念路线离线内容制品库领域约定

    内容包发布后不可变，失败制品隔离时不影响已验证版本。

    ## 核心对象

    - 素材：讲解稿摘要、音频元数据和站点关系
- 发布包：素材集合及完整摘要
- 设备能力：存储、编码和当前指纹
- 回执：下载与本地校验结果

    ## 业务边界

    - 不兼容设备只能收到拒绝原因
- 摘要失败制品必须隔离
- 覆盖率按站点和设备能力计算

    所有时间字段使用带时区的 ISO 8601 表示；公开标识与数据库自增键分离。接口返回的原因码必须稳定，便于调用方记录和重试。
